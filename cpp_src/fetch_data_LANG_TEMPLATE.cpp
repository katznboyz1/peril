#include <string>

#include "stdfunctions.hpp"

#include "fetch_data_LANG_TEMPLATE.hpp"

using namespace std;

/*
    Do all of the language data downloading here
    Use `<curl/curl.h>` to download the language data from the internet, as it already exists
    Do NOT modify other languages' data, only modify the language data associated with this files fetch_data_LANG_XXX.hpp file!
*/
int fetch_data_LANG_TEMPLATE_all(const std::string &category_data_path, const std::string &tmp_download_path, const int &curl_timeout_seconds) {

    return 0;
}